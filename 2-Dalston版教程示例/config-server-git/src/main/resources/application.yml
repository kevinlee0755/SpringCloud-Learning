spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: http://git.oschina.net/didispace/config-repo-demo/
          basedir: config
#          search-paths: repo
#          username:
#          password:

server:
  port: 1201
